<UserControl x:Class="KEI.UI.Wpf.Controls.BrowseTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ap="clr-namespace:KEI.UI.Wpf.AttachedProperties"
             mc:Ignorable="d" 
             d:DesignHeight="94.386" d:DesignWidth="759.004">
    
    <Grid DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <TextBox Text="{Binding Path, UpdateSourceTrigger=PropertyChanged}"
                 x:Name="pathTextBox"
                 TextWrapping="Wrap"
                 VerticalContentAlignment="Center">
        </TextBox>

        <Button Grid.Column="1"
                Click="Browse_Click"
                ap:IconManager.Icon="Ellipsis16x"/>

    </Grid>
</UserControl>
